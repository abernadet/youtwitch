{% block content %}
    {# url #}
    {% if results %}

        {% for result in results %}
            <a href="{{ path('OwnerChan', {channelId: result.snippet.channelId}) }}" class="list-group-item list-group-item-action"> |
                <img src="{{ result.snippet.thumbnails.default.url }}" style="padding: 5px">{{  result.snippet.title }}</a>
        {% endfor %}

    {% endif %}

{% endblock %}
