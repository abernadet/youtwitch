{% block content %}
    {# url #}
    {% if results %}

        {% for result in results %}
            <a href="{{ path('OwnerChan', {channelId: result.snippet.channelId}) }}" class="list-group-item list-group-item-action"> | {{  result.snippet.title }}
                <img src="{{ result.snippet.thumbnails.default.url }}" style="padding: 5px"></a>
        {% endfor %}

    {% endif %}

{% endblock %}
